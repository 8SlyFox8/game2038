services:
  frontend:
    container_name: game
    image: node:lts
    working_dir: /var/www/html/app/game
    ports:
      - 8000:8000
    volumes:
      - ./app/:/var/www/html/app
    tty: true
    command:
      npm run dev